# 安全策略

## 安全设计

- **Token 加密** — 所有 LLM API Key 使用 AES-256-GCM 加密存储（`token_vault.py`），永不记录日志或出现在错误信息中
- **JWT 认证** — API 与媒体端点统一 JWT 鉴权
- **会话隔离** — Redis 存储的 OAuth 会话，TTL 24 小时
- **项目隔离** — 所有知识、会话、记忆查询强制 `project_id` 作用域，禁止跨项目访问
- **最小权限** — 智能体以个人身份与个人令牌执行，权限可追责

## 报告漏洞

请使用 GitHub 的**私有漏洞报告**功能（仓库页 → Security → Report a vulnerability），不要公开发布未修复漏洞的细节。

我们承诺：

- 收到报告后 **72 小时内**确认
- 确认有效后尽快修复，并在修复后发布安全通告
- 报告期间保持沟通透明

## 建议的部署基线

- 生产环境务必关闭 `AUTH_BYPASS_*` 配置，启用 SSO
- 修改默认的 `DB_PASSWORD` 与 `SECRET_KEY`
- API 端口不要直接暴露公网，经反向代理 / 网关统一入口
